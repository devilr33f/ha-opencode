{
  "config": {
    "step": {
      "user": {
        "title": "Connect to OpenCode Go",
        "description": "Enter your workspace ID and auth cookie to monitor quota usage.\n\nFind the auth cookie in your browser devtools (Application > Cookies > opencode.ai > auth).",
        "data": {
          "workspace_id": "Workspace ID",
          "auth_cookie": "Auth Cookie"
        }
      }
    },
    "error": {
      "cannot_connect": "Failed to connect. Check your workspace ID and auth cookie.",
      "invalid_auth": "Auth cookie rejected. It may have expired."
    },
    "abort": {
      "already_configured": "This workspace is already configured."
    }
  }
}
